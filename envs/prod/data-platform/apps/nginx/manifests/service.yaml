apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: app-nginx
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: nginx
  ports:
    - port: 80
      targetPort: http
      name: http
